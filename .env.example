# Anthropic API Key（OAuth認証を使用する場合は空のままでOK）
# OAuth認証の場合、ホストの ~/.claude がマウントされるため設定不要
ANTHROPIC_API_KEY=

# macOSでSSH Agent Forwardingを使用する場合:
#   Docker Desktop の設定で "Allow the default Docker socket to be used" を有効にしてください
#   macOSのSSH_AUTH_SOCKは自動的に /run/host-services/ssh-auth.sock にマッピングされます
